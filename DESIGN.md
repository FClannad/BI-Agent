# DeepBI Design Doc

Primary design doc (Chinese): `设计.md`

This file exists as an ASCII entrypoint so the repository remains easy to browse on systems/tools that don’t handle non‑ASCII filenames well.

